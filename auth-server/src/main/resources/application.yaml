server:
  port: 8880
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/auth
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: none